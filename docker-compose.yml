services:
  web:
    build:
      context: .
      dockerfile: dockerfile
      target: production
    container_name: cfrms_web
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
